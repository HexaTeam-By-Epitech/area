name: CI Backend

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test_backend:
    name: Unit tests (area-backend)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: area-backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: area-backend/package.json

      - name: Install dependencies
        run: npm i

      - name: Run unit tests
        run: npm test --silent

  build_backend:
    name: Build (area-backend)
    runs-on: ubuntu-latest
    #needs: test_backend
    if: |
      (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev')) ||
      (github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'dev'))
    defaults:
      run:
        working-directory: area-backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: area-backend/package.json

      - name: Install dependencies
        run: npm i

      - name: Build
        run: npm run build

  build_mobile:
    name: Build (area-mobile)
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev')) ||
      (github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'dev'))
    defaults:
      run:
        working-directory: area-mobile
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: area-mobile/package.json

      - name: Install dependencies
        run: npm i

      - name: Run unit tests (jest-expo)
        run: npm test --silent

      - name: Build (Expo export for Android bundle)
        env:
          CI: true
        run: npx expo export --platform android --output-dir dist-android

  build_web:
    name: Build (area-web)
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev')) ||
      (github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'dev'))
    defaults:
      run:
        working-directory: area-web
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: area-web/package.json

      - name: Install dependencies
        run: npm i

      - name: Build
        run: npm run build
