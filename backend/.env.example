DB_USER=db-user
DB_NAME=db-name
DB_HOST=localhost
DB_PORT=4242
DB_PASSWORD=your-super-secret-password
DATABASE_URL="postgresql://db-user:your-super-secret-password@localhost:4242/db-name"

REDIS_URL="redis://localhost:6379"
REDIS_PASSWORD="mypassword"

SMTP_HOST="mail.smtp2go.com"
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

BACKEND_BASE_URL="http://localhost:3000"
FRONTEND_BASE_URL="http://localhost:5173"
MOBILE_REDIRECT_URI="area://oauth"
JWT_SECRET=""

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:3000/auth/google/callback"
GOOGLE_IDENTITY_REDIRECT_URI="http://localhost:3000/auth/google/login/callback"

SPOTIFY_CLIENT_ID=""
SPOTIFY_CLIENT_SECRET=""
SPOTIFY_REDIRECT_URI="http://localhost:3000/auth/spotify/callback"

# Slack OAuth Configuration (Hybrid: Bot + User Tokens)
SLACK_CLIENT_ID=""
SLACK_CLIENT_SECRET=""
SLACK_REDIRECT_URI="http://localhost:3000/auth/slack/callback"

# Symmetric secret used to encrypt OAuth access/refresh tokens
# AES-256-GCM key is derived as SHA-256(TOKENS_ENC_KEY). Keep this value stable.
# Changing it will invalidate the ability to decrypt previously stored tokens.
# Example (use a long random string):
# TOKENS_ENC_KEY="p3k0Z7nJqVf1wB8xL2mD9sT4yU6rH0cN"
TOKENS_ENC_KEY=""